{
  "name": "betfair-trading-agent",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "start": "npm run start -w apps/api & npm run start -w apps/web",
    "setup": "npm install && npm run prisma:generate -w apps/api && npm run prisma:push -w apps/api"
  }
}